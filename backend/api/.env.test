# Test Environment Configuration
# Digunakan saat menjalankan unit tests

# Application
PORT=3001
HOST=localhost
NODE_ENV=test

# Database (Test Database)
DB_HOST=localhost
DB_PORT=5433
DB_SYNCHRONIZE=false
DB_LOGGING=false
POSTGRES_USER=postgres_test
POSTGRES_PASSWORD=test_password
POSTGRES_DB=aparat_test

# Application Info
APP_NAME=Aparat Backend API - TEST
APP_VERSION=1.0.0-test

# JWT (Test Tokens)
JWT_SECRET=test_jwt_secret_key_change_in_production
JWT_EXPIRES_IN=30m
JWT_REFRESH_SECRET=test_refresh_secret_key
JWT_REFRESH_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000